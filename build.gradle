group 'com.sxdsf.alliance'
version '1.0.0'

apply plugin: 'groovy'
apply plugin: 'maven'

sourceCompatibility = 1.8

dependencies {
    compile gradleApi()
    compile localGroovy()
    compile 'com.android.tools.build:gradle:2.2.2'
}

repositories {
    jcenter()
}

uploadArchives {
    repositories {
        mavenDeployer {
//            repository(url: 'http://100.66.168.133:8081/nexus/content/repositories/thirdparty') {
//                authentication(userName: "admin", password: "admin123")
//            }
            repository(url: uri('../repo'))
        }
    }
}
